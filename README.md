# TODOs
1. Move some global variables in the .env file (DAYS in
`handlers/schedule.py`, TIME_FORMAT and LESSON_DURATION_MINUTES 
in `utils/formatters/schedule.py`, DAYS in `handlers/calendar.py`)
2. When the third-party API will be ready create a module
which gets a data from the API and change the code in
`handlers/schedule.py`
3. Create WebHook
